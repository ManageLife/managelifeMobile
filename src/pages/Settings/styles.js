import { StyleSheet } from 'react-native'
import { COLORS, DEVICE } from '../../config'

const styles = StyleSheet.create({
   container: {
      flex: 1,
      alignItems: 'center',
   },
   button: {
      width: 100,
   },
})

export { styles }
